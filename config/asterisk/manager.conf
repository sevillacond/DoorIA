; ==============================================================================
; ENLACE-DOORIA: ASTERISK MANAGEMENT INTERFACE (AMI) - CONFIGURAÇÃO SEGURA
; ==============================================================================
; As credenciais do usuário AMI são injetadas a partir das variáveis de ambiente
; (ASTERISK_AMI_USERNAME e ASTERISK_AMI_SECRET) pelo deploy.sh ou orquestrador.
; NENHUM segredo padrão/conhecido ou default é permitido no repositório.
; ==============================================================================

[general]
enabled = yes
port = 5038
bindaddr = 127.0.0.1
displayconnects = no
